function solve(num){

    let arr = '';
    for(let i = 0; i < num; i++){
        arr = num + ' ';
        for(let j = num; j < i.length; j++){

        }

        console.log(arr.repeat(num));
    }
}

solve(7);